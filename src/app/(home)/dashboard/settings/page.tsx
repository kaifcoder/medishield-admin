import { AdminSettings } from "@/components/component/admin-settings";
import React from "react";

type Props = {};

const page = (props: Props) => {
  return (
    <div>
      <AdminSettings />
    </div>
  );
};

export default page;
